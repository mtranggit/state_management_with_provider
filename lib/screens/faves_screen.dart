import 'package:flutter/material.dart';

class FavesScreen extends StatelessWidget {
  const FavesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Faves Screen'),
    );
  }
}
